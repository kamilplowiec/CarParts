@{
    ViewBag.Title = "Kokpit";

    int shipmentStatusId = 0;

    if(CarParts.Helpers.CurrentUser.Warehouseman)
    {
        shipmentStatusId = (int)CarParts.Enums.ShipmentStatus.PartsOrdered;
    }
    else if (CarParts.Helpers.CurrentUser.Seller)
    {
        shipmentStatusId = (int)CarParts.Enums.ShipmentStatus.New;
    }

    var customerList = string.Empty;
    if (ViewBag.CustomerList != null)
    {
        customerList = Json.Encode(((List<SelectListItem>)ViewBag.CustomerList).Select(c => new { DisplayText = c.Text, Value = c.Value }).OrderBy(s => s.Value));
    }
}

@Html.Partial("../Shared/_Messages")

<div class="main">
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <i class="fa fa-bar-chart-o fa-fw"></i> Statystyki <i></i>
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <div class="row">
                        <div class="col-sm-12">
                            
                        </div>
                    </div>
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
    </div>
</div>


@section Scripts{

    <script>
        $(function () {

        });
    </script>

    <style>
    </style>

    @Scripts.Render("~/mainJS")
}